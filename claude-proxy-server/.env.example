# Server port (optional, defaults to 3001)
PORT=3001

# You can optionally set a default Claude API key here
# But it's more secure to pass it from the client
# CLAUDE_API_KEY=your-claude-api-key-here